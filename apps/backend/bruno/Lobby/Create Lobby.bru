meta {
  name: Create Lobby
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}api/lobby/create
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Test"
    
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
